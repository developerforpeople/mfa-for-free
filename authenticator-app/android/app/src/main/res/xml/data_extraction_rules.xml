<?xml version="1.0" encoding="utf-8"?>
<!--
  Backup and transfer policy for NLR Authenticator.

  Everything is excluded. An authenticator holds the shared secrets that produce
  your one-time codes; a secret that silently rides a cloud backup onto a new
  phone is a secret that exists in a second place you did not choose.

  The practical consequence is deliberate and worth understanding: if you lose
  this device, the enrolments are gone and you recover with your recovery codes.
  That is the correct trade for a second factor. "Something you have" stops
  meaning anything if it restores itself onto anything.

  Requires targetSdk 31+ (Android 12). `allowBackup="false"` in the manifest
  covers older releases.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
